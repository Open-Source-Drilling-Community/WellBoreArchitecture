apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "osdcdrillingwellborearchitectureservice.fullname" . }}-test-connection"
  labels:
    {{- include "osdcdrillingwellborearchitectureservice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "osdcdrillingwellborearchitectureservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
